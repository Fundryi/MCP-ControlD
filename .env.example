# Copy to .env (gitignored) and fill in. Never commit the real value.
# Create a token in the Control D dashboard; use a read-scoped token unless
# you specifically need write tools.
CONTROLD_API_TOKEN=YOUR_API_TOKEN
